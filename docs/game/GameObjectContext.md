# Legacy game object context note
The current source tree does not expose a standalone object context layer.
Game-object data is represented through `GameObjectConfig`, `GameObjectKind`, and the surrounding game state.
## Use these current types instead
- `GameObjectConfig`
- `GameObjectKind`
- `GameInstance`
