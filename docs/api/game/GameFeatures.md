# Legacy game feature note
The current game API does not expose a separate feature stack.
Compose game behavior with `GameManager`, `GameDefinition`, `GameInstance`, `GameConfig`, `GameData`, `GameState`, and `GameStateMachine`.
